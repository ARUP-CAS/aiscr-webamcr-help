```{=html}

<div class="list grid quarto-listing-cols-5"">

<% for (const tile of items) { %>
  <a class="quarto-grid-link" href="<%- tile.href %>" target=_self>
    <div class="quarto-grid-item card h-100 w-200">
      <% if (tile.image) { %>
        <div class="listing-item-img-placeholder card-img-top" style="height: 100px;">
          <img class="thumbnail-image card-img" src="<%- tile.image %>" style="height:100px;">
        </div>
      <% } %>
      <div class="card-body post-contents">
        <h5 class="no-anchor card-title listing-title"><%= tile.title %></h5>
        <div class="card-text listing-description">
          <small> 
            <%= tile.description %> 
          </small>
        </div>
      </div>
      <% if (tile.url) { %>
        <div class="card-footer" style="color:#85150F; text-decoration-line: underline;">
          <small>
            <%- tile.url %>
          </small>
        </div>
      <% } %>
    </div>
  </a>

<% } %>

</div>

```
